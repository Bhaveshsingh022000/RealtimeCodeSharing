import React from 'react';

const textLines = (props) => {
    return (
        <React.Fragment>
            <span>1</span><span>ajsjkcan</span>
        </React.Fragment>
    );
}

export default textLines;